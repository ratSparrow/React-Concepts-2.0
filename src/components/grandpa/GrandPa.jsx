const GrandPa = () => {
  return <div></div>;
};

export default GrandPa;

/*
 * 1. Create a context and export it
 * 2. Add Provider with a context to the value
 * 3. useContext to access value in the context api
 */
